# Ultra-minimal requirements.txt for Railway deployment
Flask==2.3.3
Werkzeug==2.3.7
gunicorn==21.2.0

# Only database packages will be installed on Railway automatically
# psycopg2-binary will be provided by Railway's PostgreSQL buildpack